// LangGraph node that combines SQL volume trends with Pinecone RAG for 30-day pattern synthesis.
export async function detectPatterns(_competitorId: string): Promise<Record<string, unknown>> {
  return {};
}
