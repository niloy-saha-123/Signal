// Recharts line chart visualizing mention volume and sentiment trends over a 30-day window.
"use client";

export function TrendChart({ competitorId }: { competitorId: string }) {
  return <div data-competitor={competitorId}>Trend chart — not yet implemented</div>;
}
