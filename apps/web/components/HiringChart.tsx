// Recharts bar chart showing department-level hiring velocity deltas from job posting signals.
"use client";

export function HiringChart({ competitorId }: { competitorId: string }) {
  return <div data-competitor={competitorId}>Hiring chart — not yet implemented</div>;
}
