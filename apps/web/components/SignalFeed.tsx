// Real-time signal feed component that listens for new signals via Socket.io WebSocket events.
"use client";

export function SignalFeed({ competitorId }: { competitorId: string }) {
  return <div data-competitor={competitorId}>Signal feed — not yet implemented</div>;
}
